# 設定 Windows 排程,每天自動執行

先選一種你想要的模式,再照對應的步驟設定。

- **模式A(自動更新+自動跳出來看)**:到時間自動抓資料、算勝率,並自動跳出瀏覽器顯示。
- **模式B(自動更新+自己找時間手動看)**:到時間自動在背景抓資料、算勝率、存成網頁檔,但不會自動跳出來,你想看的時候自己雙擊 `report.html` 開。

> 前提:電腦在排定的時間必須是**開機且已登入**狀態(不用開著視窗,但不能是關機或睡眠)。

---

## 模式A:自動更新 + 自動跳出瀏覽器

### 步驟

1. 按 **開始鍵**,搜尋「**工作排程器**」(Task Scheduler)並打開。
2. 右上角點「**建立基本工作...**」(Create Basic Task)。
3. 名稱填 `MLB每日預測`,點「下一步」。
4. 觸發程序選「**每天**」(Daily),點「下一步」,設定時間(例如早上 8:00),點「下一步」。
5. 動作選「**啟動程式**」(Start a program),點「下一步」。
6. 「程式或指令碼」欄位,點「瀏覽」選到 `每日執行.bat`,點「下一步」。
7. 檢查摘要無誤後點「完成」。

### 加碼:順便設定「自動核對昨天命中率」

重複上面 1~7,差別只有:名稱填 `MLB核對命中率`、時間設**隔天早上**(例如9:00,確保昨晚比賽都打完)、步驟6選 `核對昨日戰績.bat`。

設定好之後,你每天早上會有兩份網頁自動跳出來:今天的預測 + 昨天猜得準不準。

---

## 模式B:自動更新 + 自己手動點開看(你要的就是這個)

只需要設定**一個**排程,指向 `背景更新.bat`,它會自動幫你做兩件事(更新今天預測 + 核對昨天命中率),但全部安靜寫進檔案,不會自動彈出來打擾你。

### 步驟

1. 按 **開始鍵**,搜尋「**工作排程器**」並打開。
2. 右上角點「**建立基本工作...**」。
3. 名稱填 `MLB背景更新`,點「下一步」。
4. 觸發程序選「**每天**」,設定時間(建議早上 8~9 點,確保先發投手名單和前一晚比分都已公布),點「下一步」。
5. 動作選「**啟動程式**」,點「下一步」。
6. 「程式或指令碼」欄位,瀏覽選到 `背景更新.bat`,點「下一步」。
7. 完成前勾選「**開啟這個工作的內容對話方塊**」,點「完成」。
8. 在跳出的內容對話方塊裡,切到「**一般**」分頁,勾選「**不論使用者登入與否均執行**」(Run whether user is logged on or not)——這樣執行時才不會跳出黑色視窗閃一下。存檔時可能要求輸入你的Windows登入密碼。

### 之後怎麼看結果?

打開 `mlb_predictor` 資料夾,直接雙擊裡面的:
- **`report.html`** → 看最新一次更新的賽事預測
- **`verify_report.html`** → 看最新一次核對的命中率

這兩個檔案每次背景更新都會覆蓋成最新內容,你隨時想看就雙擊打開,不會受排程有沒有跑影響操作。

### 想確認排程到底有沒有正常執行?

打開 `mlb_predictor` 資料夾裡的 **`update_log.txt`**,裡面會記錄每次執行的時間與結果(成功或錯誤訊息),方便回頭確認。

---

## 兩種模式都適用的說明

### 驗證有沒有設定成功

在工作排程器左側「工作排程器程式庫」找到你設定的工作,右鍵選「**執行**」,測試看看是否正常(模式A會跳出瀏覽器;模式B去看 `update_log.txt` 有沒有新的一行)。

### 之後想調整時間

在工作排程器程式庫裡找到該工作,雙擊打開,切到「觸發程序」分頁編輯即可。

## 常見問題

**Q: 到了時間但沒有反應?**
A: 確認電腦當時是開機且已登入(排程預設不會喚醒睡眠中的電腦,除非在「條件」分頁勾選「喚醒電腦執行此工作」)。

**Q: 模式B執行時還是會閃一下黑色視窗?**
A: 就算勾選了「不論使用者登入與否均執行」,某些 Windows 版本仍可能短暫閃現。若想完全消除,可以額外做一個 VBS 靜默啟動器,需要的話跟我說,我再幫你寫。

**Q: 想同時收到 Email 通知,不用自己想起來去點開?**
A: 這需要額外設定 SMTP 寄信,如果需要我可以再幫你寫這段功能。
