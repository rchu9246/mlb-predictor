# 雲端自動化版設定教學

這個版本用 **GitHub Actions**(GitHub免費提供的雲端排程機器人)每天自動幫你跑預測、核對戰果、更新準確率統計,再用 **GitHub Pages**(GitHub免費提供的網頁託管)產生一個永久網址,手機、電腦、平板隨時開瀏覽器就能看——完全不需要開著自己的電腦。

全部都是**免費**的,只需要一個GitHub帳號(如果沒有,前面步驟會教你申請)。

## 整體概念

```
GitHub Actions(每天台北時間早上9點自動執行)
  → 抓MLB即時戰績+對戰表+先發投手
  → 算出勝率預測
  → 順便核對昨天猜得準不準
  → 更新累積準確率統計
  → 全部存成網頁,自動推回你的GitHub倉庫
      ↓
GitHub Pages(自動把上面產生的網頁變成一個網址)
  → https://你的帳號.github.io/mlb-predictor/
```

---

## 步驟1:申請GitHub帳號(如果還沒有的話)

前往 https://github.com/signup,用Email註冊一個免費帳號。已經有帳號的話跳過這步。

## 步驟2:建立一個新的Repository(倉庫)

1. 登入GitHub後,右上角點「+」→「New repository」
2. Repository name 填 `mlb-predictor`(或你喜歡的名字)
3. 選擇「**Public**」(公開)——GitHub Pages 免費方案需要公開倉庫才能架設網站,反正裡面只是棒球勝率預測,沒有隱私疑慮
4. 不要勾選「Add a README file」(等一下會整個上傳一份現成的)
5. 點「Create repository」

## 步驟3:把整包程式上傳到這個倉庫

這一步有兩種做法,選一種就好:

### 做法A:網頁上傳(不用裝任何軟體,適合新手)

1. 解壓縮你電腦上的 `mlb_predictor` 資料夾
2. 在剛建立的GitHub倉庫頁面,點「uploading an existing file」
3. 把解壓縮後**資料夾裡面的所有檔案和資料夾**(包含 `.github` 這個資料夾,它預設可能被電腦隱藏,要先設定顯示隱藏檔案)整個拖曳上傳
4. 下方填寫commit訊息(例如「初始上傳」),點「Commit changes」

⚠️ 注意:`.github` 資料夾在Windows檔案總管預設是隱藏的,需要在檔案總管「檢視」→勾選「隱藏的項目」才看得到,務必確認它有一起上傳,不然自動排程不會生效。

### 做法B:用Git指令上傳(如果你會用命令列)

```bash
cd mlb_predictor
git init
git add .
git commit -m "初始上傳"
git branch -M main
git remote add origin https://github.com/你的帳號/mlb-predictor.git
git push -u origin main
```

## 步驟4:啟用 GitHub Pages

1. 在倉庫頁面點「Settings」(設定)
2. 左側選單找到「Pages」
3. 「Source」選「Deploy from a branch」
4. 「Branch」選「main」,右邊資料夾選「/docs」
5. 點「Save」

存檔後過1~2分鐘,頁面上方會顯示你的網址,格式類似:
```
https://你的帳號.github.io/mlb-predictor/
```
先把這個網址記下來或加入書籤。

## 步驟5:手動觸發第一次執行(測試看看)

不用等到明天早上,可以馬上手動跑一次確認有沒有設定成功:

1. 在倉庫頁面點「Actions」分頁
2. 左側選「MLB Daily Auto Update」
3. 右邊點「Run workflow」下拉選單 → 綠色「Run workflow」按鈕
4. 等待約1分鐘,重新整理頁面,執行狀態會從黃色圈圈變成綠色勾勾(代表成功)

## 步驟6:打開你的網址看結果

執行成功後,打開步驟4記下來的網址,應該會看到首頁,點進去就能看到今日預測、核對報告、累積準確率三個頁面。

之後**每天台北時間早上9點**,GitHub會自動幫你重跑一次,網頁內容自動更新,你隨時想看就打開那個網址即可。

---

## 想調整自動執行時間?

打開 `.github/workflows/daily.yml`,找到這一行:
```yaml
    - cron: '0 1 * * *'
```
這是UTC時間01:00(=台北時間09:00)。cron格式是「分 時 日 月 星期」,例如想改成台北時間早上7點,UTC是前一天23:00,要改成:
```yaml
    - cron: '0 23 * * *'
```
改完後,一樣要上傳(commit)回GitHub才會生效。

## 常見問題

**Q: Actions頁面顯示紅色叉叉(執行失敗)怎麼辦?**
A: 點進去那次執行紀錄,展開紅色叉叉的步驟,把錯誤訊息截圖給我看。最常見原因是 `.github` 資料夾沒有正確上傳,或Settings裡的Pages沒設定好。

**Q: 免費帳號會不會有使用限制?**
A: GitHub Actions 對公開倉庫完全免費、沒有時數限制。一天跑一次、每次不到2分鐘,完全不會有問題。

**Q: 想同時做CPBL的雲端版可以嗎?**
A: CPBL官網會封鎖GitHub Actions的雲端IP,沒辦法像MLB這樣完全自動抓資料,所以目前先只做MLB。如果你想要,我可以之後幫你做一個「雲端跑計算,但你手動更新對戰表」的混合版本。

**Q: 想要準確率算出來後,直接發通知到手機或Email通知我,而不是要自己記得去看網址?**
A: 可以另外加一段用GitHub Actions寄Email或推播通知的設定,需要的話跟我說,我再幫你加上去。
